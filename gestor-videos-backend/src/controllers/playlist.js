//Controller para gestionar playlists de videos

const getPlaylist = (req, res, next) => {};

const postPlaylist = (req, res, next) => {};

const playListController = {
	getPlaylist,
	postPlaylist,
};

export default playListController;
